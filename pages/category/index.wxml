<!-- 导入搜索组件 -->
<search/>

<view class="container">
  <view class="menu">
    <view class="menu-item {{current === index ? 'active' : ''}}" 
    wx:for="{{ [1,1,1,11,1,1,1,1,11,1,1,11,1] }}"
    wx:key="{{index}}"
    bindtap="handleClick"
    data-index="{{index}}"
    >
    电器{{index}}
    </view>
  </view>
  <view class="content">
    会变的内容{{current}}
  </view>
</view> 